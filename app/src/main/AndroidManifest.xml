<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.birzeit.project1">
    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project1">
        <activity
            android:name=".prelogin.LoginActivity"
            android:exported="false" /> <!-- Activities -->
        <activity
            android:name=".prelogin.RegistrationActivity"
            android:exported="false" />
        <activity
            android:name=".student_fragments.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.Project1NoAction" />
        <activity
            android:name=".librarian_fragment.MainLibrarianActivity"
            android:exported="false" />
        <activity
            android:name=".librarian_fragment.MainLibrarianActivity"
            android:exported="false" />
        <activity
            android:name=".prelogin.WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>